<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name='robots' content='index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1'>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<% if(typeof seo.description !='undefined' ) {%><%= seo.description %><% } else { %>No description<% } %>">
    <meta property="og:locale" content="pt_BR" />
    <meta property="og:type" content="website" />
    <meta property="og:title" content="<% if(typeof seo.title !='undefined' ) {%><%= seo.title %><% } else { %>No title<% } %>">
    <meta property="og:url" content="<% if(typeof seo.canonical !='undefined' ) {%><%= seo.canonical %><% } else { %>No url<% } %>">
    <meta property="og:site_name" content="<% if(typeof seo.site_name !='undefined' ) {%><%= seo.site_name %><% } else { %>No name<% } %>">
    <meta property="og:image" content="<% if(typeof seo.cover !='undefined' ) {%><%= seo.cover %><% } else { %>No cover<% } %>">
    <title><% if(typeof seo.title !='undefined' ) {%><%= seo.title %><% } else { %>No title<% } %></title>
    <link rel="canonical" href="<% if(typeof seo.canonical !='undefined' ) {%><%= seo.canonical %><% } else { %>No url<% } %>" />
    <link rel="shortcut icon" href="<% if(typeof seo.icon !='undefined' ) {%><%= seo.icon %><% } else { %>No icon<% } %>" type="image/x-icon">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Material+Icons|Material+Icons+Outlined|Material+Icons+Two+Tone|Material+Icons+Round|Material+Icons+Sharp">
    <link rel="stylesheet" href="/css/global.css">
    <% if (typeof files !='undefined' ) { %>
        <% files.forEach((file)=> { %>
            <%- include('styleTag', {file: file}) %>
        <% }); %>
    <% } %>
</head>

<body>